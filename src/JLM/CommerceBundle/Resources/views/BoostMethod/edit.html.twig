{% extends "JLMCommerceBundle:BoostMethod:layout.html.twig" %}

{% block content -%}
    <h1>{{ 'BoostMethod edit'|trans }}</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('boostmethod') }}">
            {{ 'Back to the list'|trans }}
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}

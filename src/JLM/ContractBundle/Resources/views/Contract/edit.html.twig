{{ form_start(edit_form, {'attr' : {
    'class': 'form-horizontal modal hide fade',
    'role': 'dialog',
    'aria-hidden': 'true',
    'id': 'modalEdit' ~ entity.id,
    'aria-labelledby': 'modalEditLabel' ~ entity.id }
} )
}}
    <div class="modal-header">
		<button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
		<h3 id="modalEditLabel{{ entity.id }}">Edition d'un contrat</h3>
	</div>
	<div class="modal-body">
		{{ form_widget(edit_form) }}
	</div>
	<div class="modal-footer">
		<button class="btn" data-dismiss="modal" aria-hidden="true">Fermer</button>
		<button type="submit" class="btn btn-primary"><i class="icon-pencil icon-white"></i> Enregistrer</button>
	</div>
{{ form_end(edit_form) }}

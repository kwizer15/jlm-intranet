{% extends "JLMDailyBundle::layout.html.twig" %}

{% block content %}
<h1>Intallations à l'arrêt <small>({{ entities | length }})</small></h1>
	{% if entities|length == 0  %} 
		<h2><small>Aucun résultat</small></h2>
	{% else %}
		<h2><small>Intallations</small></h2>
		{% include 'JLMDailyBundle:Door:list.html.twig' with {'doors': entities } %}
	{% endif %}
	<div class="form-actions">
<a href="{{ path('daily_door_printstopped') }}" class="btn btn-info"><i class="icon-white icon-print"></i> Imprimer</a>
</div>
{% endblock %}
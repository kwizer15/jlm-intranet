{# JLMDailyBundle:Fixing:show.html.twig #}
{% extends "JLMDailyBundle:Intervention:show.html.twig" %}

{% block intervention_type %}Dépannage{% endblock %}

{% block reportclosed %}
	 <tr><th>Cause</th><td>{{ entity.due }}</td></tr>
	 <tr><th>Action menée</th><td>{{ entity.done }}</td></tr>
	 {{ parent() }}
{% endblock %}

{% block actions %}
    <a href="{{ path('fixing_list') }}" class="btn"><i class="icon-arrow-left"></i> Retour à la liste</a>
    <a href="{{ path('fixing_edit', { 'id': entity.id }) }}" class="btn btn-warning"><i class="icon-pencil icon-white"></i> Editer</a>
	<a href="#modalClose{{ entity.id }}" role="button" data-toggle="modal" class="btn btn-primary"><i class="icon-ok icon-white"></i> {% if not entity.closed %}Cloturer{% else %}Editer cloture{% endif %}</a>
	{% render 'JLMDailyBundle:Fixing:close' with {'id':entity.id} %}
	{{ parent() }}
</div>
{% endblock %}
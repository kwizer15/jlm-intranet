{% extends "JLMModelBundle::layout.html.twig" %}

{% block content %}
<h1>Nouveau contrat</h1>
<address>
<strong>Affaire</strong>
<br>{{ entity.door | nl2br }}
</address>
<form action="{{ path('contract_create') }}" method="post" {{ form_enctype(form) }} class="form-horizontal">
    <fieldset>
    {{ form_widget(form) }}
	<div class="form-actions">
	   <a href="{{ path('door_show', {'id':entity.door.id}) }}" class="btn"><i class="icon-arrow-left"></i> Retour à la liste</a>
	   <button type="submit" class="btn btn-primary"><i class="icon-pencil icon-white"></i> Enregistrer les modifications</button>
	</div>
</fieldset>
{% endblock %}


{% block javascripts %}
{{ parent() }}
<script src="{{ asset("bundles/jlmmodel/js/contract.js") }}"></script>
<script>
$("body").contract();
</script>
{% endblock %}
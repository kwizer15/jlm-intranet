<h1>DocumentContentText creation</h1>

<form action="{{ path('documentcontenttext_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('documentcontenttext') }}">
            Back to the list
        </a>
    </li>
</ul>

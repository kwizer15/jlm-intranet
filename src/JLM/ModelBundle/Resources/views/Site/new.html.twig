{% extends "JLMModelBundle::layout.html.twig" %}
{% block content %}
<h1>Nouvelle affaire</h1>
<form action="{{ path('site_create') }}" method="post" {{ form_enctype(form) }} class="form-horizontal">
<fieldset>
{{ form_widget(form) }}
    <div class="form-actions">
	   <a href="{{ path('site') }}" class="btn"><i class="icon-arrow-left"></i> Retour à la liste</a>
	   <button type="submit" class="btn btn-primary"><i class="icon-plus-sign icon-white"></i> Créer</button>
	</div>
</fieldset>
{% endblock %}
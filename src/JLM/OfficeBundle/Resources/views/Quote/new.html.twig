{% extends "JLMOfficeBundle::layout.html.twig" %}
{% form_theme form 'JLMOfficeBundle:Form:field_row.html.twig' %}
{% block content %}
<h1>Nouveau devis</h1>
{% if form_errors(form) %}
<div class="alert">{{ form_errors(form) }}</div>
{% endif %}
<form action="{{ path('quote_create') }}" method="post" {{ form_enctype(form) }} class="form-horizontal">
    <fieldset>
    {{ form_widget(form) }}
	<div class="form-actions">
	 
	   <button type="submit" class="btn btn-primary"><i class="icon-pencil icon-white"></i> Enregistrer les modifications</button>
	</div>
</fieldset>
{% endblock %}

{% block javascripts %}
{{ parent() }}
<script src="{{ asset("bundles/jlmoffice/js/quote.js") }}"></script>
<script>
$("body").quote({
	 followersSource:'',
	 doorsSource: "{{ path('quote_auto_door') }}",
	 trusteesSource:'',
	 paymentSource:'{{ path('quote_auto_payment') }}',
	 delaySource:'{{ path('quote_auto_delay') }}',
	 introSource:'{{ path('quote_auto_intro') }}',
	 	 
	 lineCount:{{ form.lines | length }},
	 lineReferenceSource:'{{ path('quote_auto_product_reference') }}',
	 lineDesignationSource:'{{ path('quote_auto_product_designation') }}',
});
</script>
{% endblock %}
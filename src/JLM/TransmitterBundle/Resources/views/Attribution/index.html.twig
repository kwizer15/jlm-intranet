{% extends "JLMTransmitterBundle:Attribution:layout.html.twig" %}

{% block content %}
<h1>Attributions d'émetteurs</h1>
<table class="table">
    <thead>
        <tr>
            <th>Date</th>
            <th>Affaire</th>
            <th>Contact</th>
            <th>Particulier</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td>{% if entity.creation %}{{ entity.creation|date('d/m/Y') }}{% endif %}</td>
            <td><a href="{{ path('transmitter_attribution_show', { 'id': entity.id }) }}">{{ entity.ask.site }}</a></td>
            <td>{{ entity.contact }}</td>
            <td>{% if entity.individual %}<i class="icon-ok"></i>{% endif %}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('transmitter_attribution_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('transmitter_attribution_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}
